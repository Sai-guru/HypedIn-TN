# Example environment variables for the admin-auth-server
# Copy this file to `.env` and fill in real values before running the server.
# DO NOT commit your real `.env` to version control.

# Server
PORT=5000
NODE_ENV=development

# MongoDB
# Example (local): mongodb://localhost:27017/admin_auth
# Example (Atlas): mongodb+srv://<user>:<pass>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/admin_auth

# JWT secrets
# Use long, random strings in production
JWT_SECRET=replace-this-with-a-strong-secret
JWT_REFRESH_SECRET=replace-this-with-a-strong-refresh-secret

# SMTP (for sending OTP / reset emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-smtp-username@example.com
SMTP_PASS=your-smtp-password
FROM_EMAIL=noreply@yourdomain.com

# Cloudinary (for uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Optional safety/timeouts / extra config
# e.g. increase timeouts or add feature flags
#EXTRA_FEATURE_FLAG=false
